{
  "scripts": {
    "clean": "rm -rf node_modules frontend/node_modules && rm -f package-lock.json frontend/package-lock.json",
    "install:all": "npm install && cd frontend && npm install",
    "build:contracts": "cd contracts/meltyfi && sui move build",
    "test:contracts": "cd contracts/meltyfi && sui move test",
    "deploy:devnet": "cd contracts/meltyfi && sui client publish --gas-budget 100000000",
    "dev:frontend": "cd frontend && npm run dev",
    "build:frontend": "cd frontend && npm run build",
    "start": "concurrently \"npm run dev:frontend\"",
    "fix:duplicates": "cd contracts/meltyfi && find sources -name '*.move' -exec sed -i '' -e 's/use sui::transfer;//g' -e 's/use sui::tx_context::{Self, TxContext};//g' -e 's/use sui::object::{Self, UID, ID};//g' {} +"
  }
}