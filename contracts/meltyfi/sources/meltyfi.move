/// Main entry point and module organization for MeltyFi Protocol
module meltyfi::meltyfi {
    // Simple entry point - no re-exports needed for now
    // Users will call functions directly on individual modules
    
    // This module serves as documentation and initialization point
    use meltyfi::meltyfi_core;
    use meltyfi::choco_chip;  
    use meltyfi::wonka_bars;

    // Module functions can be added here for convenience
    // but for now we keep it simple to avoid compilation issues
}